#ifndef Matrix_hpp
#define Matrix_hpp

template <typename K>
struct Matrix {

	/*
	• A function to return the size/shape of a vector/matrix.
	• A function to tell if a matrix is square.
	• A function to print a vector/matrix on the standard output.
	• A function to reshape a vector into a matrix, and vice-versa.
	*/

	/*
	You must write functions that can add and subtract two vectors, or two matrices, of same size;
	and a function to multiply a vector, or a matrix, by a scalar (ie, "scaling")
	*/

	/*
	If the mathematical operation is nonsensical (ie, summing a vector and a scalar, or vectors of different sizes),
	the result is undefined.
	*/

	/*
	impl<K> Matrix<K> {
		fn add(&mut self, v: &Matrix<K>);
		fn sub(&mut self, v: &Matrix<K>);
		fn scl(&mut self, a: K);
	}
	*/
};

#endif